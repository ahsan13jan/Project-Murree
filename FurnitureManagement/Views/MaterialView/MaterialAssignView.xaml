<Page x:Class="FurnitureManagement.Views.MaterialView.MaterialAssignView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FurnitureManagement.Views.ItemViews"
        xmlns:my="clr-namespace:FurnitureManagement"
        mc:Ignorable="d" 
        Title="ItemsView" >
    <Grid Margin="10" >
        <Grid.Resources>
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Margin" Value="0,0,0,5" />
            </Style>

        </Grid.Resources> 
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>


        <Label Grid.Row="0" Style="{StaticResource SubHeading}" > Material Assign History</Label>


        <StackPanel Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <Label Width="100" Margin="0,0,5,0">From</Label>
                <Label Width="100" Margin="0,0,5,0">To</Label>
                <Label Width="100" Margin="0,0,5,0">Article</Label>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <DatePicker Name="Input_From" Width="100" Margin="0,0,5,0"  />
                <DatePicker Name="Input_To" Width="100" Margin="0,0,5,0"   />
                <ComboBox Name="CB_Articles" Width="100" Margin="0,0,5,0" DisplayMemberPath="Article_DESC" SelectedValuePath="Article_Id" SelectionChanged="Articles_SelectionChanged" />
                <Button Click="Update_Click"> Update</Button>
            </StackPanel>
        </StackPanel>
        <ScrollViewer Margin="5,10,0,0" Grid.Row="2" Grid.Column="0"  Height="350" >
        <Grid  >
            <DataGrid Name="dataGrid"  >
                <DataGrid.Columns>

                    <DataGridTextColumn Header="Id" Binding="{Binding Id}" />
                     <DataGridTextColumn Header="Item" Binding="{Binding Item.UIN}" />
                        <DataGridTextColumn Header="Article" Binding="{Binding Item.JobItem.Article.Article_DESC}" />
                        <DataGridTextColumn Header="Material" Binding="{Binding Material.Name}" />
                        <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" />
                        <DataGridTextColumn Header="Rate" Binding="{Binding Rate}" />
                        <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt}" />
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
        </ScrollViewer>
    </Grid>
</Page>
